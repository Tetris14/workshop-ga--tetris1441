name: Tests

on:
  push:
    branches:
      - dev
      - master

jobs:
  hello-world:
    name: Hello world
    runs-on: ubuntu-20.04

    steps:
      - name : Say Hellow world
        shell: bash
        run: echo Github actions workshop